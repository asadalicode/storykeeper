export enum Languages {
  'en-US' = 'English',
  'es-ES' = 'Spanish',
}

export enum ModalDismissRole {
  'submitted' = 'submitted',
  'canceled' = 'canceled',
  'backdroped' = 'backdroped',
}

export enum myLibraryTabs {
  'all' = '0',
  'pending' = '1',
  'inprogress' = '2',
  'published' = '3',
  'shared' = '4',
}
